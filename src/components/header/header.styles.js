import styled from 'styled-components';
import { COLORS } from '../../styles/Colors';

const StyledContainer = styled.div`
  border-bottom: 1px solid ${COLORS.primary00};
`;

export { StyledContainer };
